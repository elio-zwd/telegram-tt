node-options="--no-experimental-strip-types"
# npm 11.10+ 在准备 Git 依赖时会把 min-release-age 与内部 --before 同时传递，导致 npm ci 失败。
# 本仓库使用已提交的 package-lock.json 和 npm ci 固定依赖版本，不在项目级设置 min-release-age。
include[]=dev
include[]=optional
bin-links=true
